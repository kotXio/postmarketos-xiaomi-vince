# Device configuration

Small configuration files that accompany the Vince packages are kept here
separately from the APK source. They do not contain firmware, calibration or
phone-specific data.

- [`vince-fm`](vince-fm/README.md) adds native FM module loading, access rules
  and a qv4l2 launcher.
