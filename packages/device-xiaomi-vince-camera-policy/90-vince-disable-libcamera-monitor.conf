# Xiaomi Redmi 5 Plus (vince): Plasma Camera uses libcamera directly.
# Avoid keeping the linked DW9763 lens powered only for PipeWire discovery.
wireplumber.profiles = {
  main = {
    monitor.libcamera = disabled
  }
}
